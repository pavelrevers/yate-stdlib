// all functions returns number, so we should use xml as return type

// Math.abs(num)
external xml math-abs(scalar)

// randim 6-digit number
external xml math-random()

// Math.round()
external xml math-round(scalar)

// sum
external scalar math-sum(nodeset)

// evil!
external scalar math-procedure(scalar)

// format-price: 123456.0234 -> 123456.02
external scalar math-format-price(scalar)
